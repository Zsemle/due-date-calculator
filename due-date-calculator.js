function CalculateDueDate(submitDate,turnaroundTime){
  let resolvedBy = Date.now();
  return resolvedBy;
}

module.exports = function(submitDate,turnaroundTime){
  return CalculateDueDate(submitDate,turnaroundTime)
}
